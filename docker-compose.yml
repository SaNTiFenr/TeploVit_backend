services:

    mysql:
        image: 'mysql/mysql-server:8.0'
        ports:
            - 3306:3306
        environment:
            MYSQL_ROOT_PASSWORD: teplovit123_root
            MYSQL_USER: Root
            MYSQL_DATABASE: teplovit
            MYSQL_PASSWORD: teplovit123
        volumes:
            - 'sail-mysql:/var/lib/mysql'
            # - './vendor/laravel/sail/database/mysql/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh'
        # networks:
        #     - sail
volumes:
    sail-mysql:
        driver: local
